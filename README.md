# Spam-email-classifier
A machine learning project to detect spam emails using NLP techniques and classification algorithms. It cleans and transforms email text, trains a predictive model, and classifies emails as spam or not spam with high accuracy.
